# -*- mode: snippet -*-
# name:Template 
# key:cc 
# --

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdbool.h>

#define REP(i, n) for (int i = 0; i < (n); ++i)
#define FOR(i, k, n) for (int i = (k); i < (n); ++i)

int main() {
  $1
  return 0;
}